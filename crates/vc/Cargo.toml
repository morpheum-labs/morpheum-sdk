[package]
name = "morpheum-sdk-vc"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "VC module client for the Morpheum SDK — issue, revoke, query, update, and manage Verifiable Credentials with full TradingKeyClaim support"

[lib]
crate-type = ["lib"]

[features]
default = []
# Enables std support (required when used from native crate)
std = [
    "morpheum-sdk-core/std",
    "morpheum-proto/std",
]
# Enables serde support for VC types (e.g. Vc, VcClaims, Vp)
serde = [
    "dep:serde",
    "morpheum-sdk-core/serde",
    "morpheum-proto/serde",
]
# Convenience feature for full std + serde
full = ["std", "serde"]
# Enables host-side zkClaims commitment computation in the issue builder
# (WS3-D). Pulls the heavy dusk-plonk-backed primitive (`ai`), so it is std-only
# and opt-in — a plaintext-only client never links it.
zk = ["std", "dep:morpheum-primitives"]

[dependencies]
# Core SDK foundation (types, config, transport, client trait, error, builder)
morpheum-sdk-core = { path = "../core", default-features = false }

# Canonical protobuf definitions
morpheum-proto = { workspace = true }

# zkClaims host-side commitment (WS3-D); only with the `zk` feature.
morpheum-primitives = { workspace = true, features = ["ai"], optional = true }

# Protobuf runtime (for Message::decode / encode_to_vec)
prost = { version = "0.14", default-features = false }

# Async trait for MorpheumClient impl
async-trait = "0.1"

# Hex encoding/decoding (for AccountId address parsing)
hex = { version = "0.4", default-features = false, features = ["alloc"] }

# Optional serialization (only enabled via "serde" feature)
serde = { version = "1.0", optional = true, default-features = false, features = ["derive"] }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
